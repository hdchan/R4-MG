from AppUI.MainAssembly import *
from AppCore import *
# import cProfile
# import pstats
# import sys

if __name__ == '__main__':
    # profiler = cProfile.Profile()
    # profiler.enable()
    MainAssembly()
    # profiler.disable()
    # stats = pstats.Stats(profiler).sort_stats('cumulative')
    # stats.print_stats(100)  # Print top 10 functions by cumulative time